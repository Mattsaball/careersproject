import LoginForm from "@/components/LoginForm"; // adjust if your path differs

export default function LoginPage() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <LoginForm />
    </div>
  );
}
